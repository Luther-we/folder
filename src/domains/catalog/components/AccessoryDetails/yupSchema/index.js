// création des schémas yup
// plusieurs constantes possibles dans le même schema
// les exports sont des exports simple

import i18n from 'services/translation/i18n';
import * as yup from 'yup';

const listElementSchema = {
  // blabla: yup...
}

export const accessorryDetailsElementSchema = {
  // foo : yup....
}

export const accessorryDetailsSecondElementSchema = {
  // bar : yup....
}